import type { PerimeterFunction, Registry } from '../../../types.js';
/**
 * A registry that stores the {@link PerimeterFunction}s.
 *
 * @since 0.20.0
 * @category Style
 * @category Configuration
 */
export declare const PerimeterRegistry: Registry<PerimeterFunction>;
